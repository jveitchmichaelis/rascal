[build-system]
requires = ["poetry>=1.1", "setuptools >= 60.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = "-s -x --timeout 600"
testpaths = [
    "test"
]